$schema: "http://json-schema.org/draft-04/schema#"

description: an entry in the sample sheet
properties:
  group:
    type:
      - string
      - number
    description: sample group
  sample:
    type: string
    description: sample name/identifier
  control:
    type: string
    description: sample type, target or control
required:
  - group
  - sample
  - control
